@{
    ViewBag.Title = "Blog";
}
@using Blog.Models

@model List<Post>

<div> @Html.ActionLink("New post", "New", null, new {@class = "link-button" })</div>

 <div class="col-md-8 content-main">
      <div class="content-grid">

        @foreach (var post in Model)
        {
            @Html.Action("PostInfo", post);
        }

       </div>
 </div>
            

